version: "3.9"

services:
  app:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
    environment:
      - DB_HOST=db
      - 
  db:
    image: postgres:13-alpine
    volumns: 
      - dev-db-data:/var/lib/postgressql/data
    environment:
      - POSTGRES_DB=devdb
      - POSTGRES_USER=devuser
      - PSTGRES_PASSWORD=changeme

volumes:
  dev-db-data: 